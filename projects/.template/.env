# Project environment variables (add more as needed).
# Prefer setting values in the host environment before launching VS Code.

# Container name and workspace path.
# VSCODE_CONTAINER_PROJECT=your-project-name
